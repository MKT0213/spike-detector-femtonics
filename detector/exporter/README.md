# Exporter Program

Run the standalone exporter:

```powershell
python detector/exporter/main.py
```

This program opens an analysis package folder and exports selected tables, reload files, reports, and images.
